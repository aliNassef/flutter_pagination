name: pagination_in_details
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  bloc: ^9.0.0
  dio: ^5.9.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  get_it: ^8.2.0
  injectable: ^2.5.1

dev_dependencies:
  build_runner:  
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  injectable_generator: 

flutter:
  uses-material-design: true
